"""
baselines — reference architectures the wave operator is compared against.

Rule #1 (from prior overclaims): a comparison is only meaningful against a real,
matched, budget-matched baseline whose published number we have reproduced on our
own data. `fno.py` is the primary reference (FNO-2D, Li et al. 2020).
"""
