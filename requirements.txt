# Energy-forecasting pivot (current direction — see docs/final-plan.md)
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0
lightgbm>=4.0.0          # gradient-boosted quantile baseline (industry workhorse)
pvlib>=0.11.0           # solar physics features / baseline
entsoe-py>=0.6.0        # ENTSO-E Transparency: generation, TSO forecast, prices
requests>=2.31.0        # Open-Meteo weather (free, no key)
xarray>=2024.0.0        # NWP/reanalysis handling
cdsapi>=0.7.0           # Copernicus ERA5 (scalable weather path, later)
netCDF4>=1.6.0
pyarrow>=15.0.0         # parquet caching
matplotlib>=3.7.0
tqdm>=4.65.0

# Reused from prior work
torch>=2.1.0            # diffusion machinery (wave_field/diffusion.py) → forecast ensembles

# Legacy / parked (PDE + image/audio lineage — not needed for current direction)
# torchvision>=0.16.0
# torchaudio>=2.1.0
# soundfile>=0.12.0
# clean-fid>=0.1.35
